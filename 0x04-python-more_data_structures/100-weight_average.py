#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0

    number = 0
    index = 0

    for t in my_list:
        number += tup[0] * tup[1]
        index += tup[1]

        return (number / index)
